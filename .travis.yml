language: cpp
distro: xenial

addons:
    apt:
        packages:
            - cmake-data
            - cmake
            - libboost-dev
            - libboost-system-dev

matrix:
  include:
    - os: linux
      compiler: clang
    - os: linux
      compiler: gcc

before_install:
    - eval "${MATRIX_EVAL}"

script:
    - mkdir build
    - cd build
    - cmake ..
    - make
    - ./test/testjsring
